# Apache Answer route shell reduction

This fixture reduces Apache Answer's browser router, `Layout`, and
`SideNavLayout` at `3b9f1370612e690a0b7f230f05e688930db4c6d3` (Apache-2.0) to
Kudzu file routes, ordinary layout composition, native anchors, and runtime
pathname parameters. Every route remains directly addressable without a router
runtime.
