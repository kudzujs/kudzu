export function nextId(length: number) {
  return length + 1
}

export const importedTitle = "Imported"
export const removeType = "remove"

export function normalizeTitle(title: string) {
  return title.trim()
}
